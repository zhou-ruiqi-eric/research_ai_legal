name: Auto Update Folder Tree

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  update-tree:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install tree command
        run: sudo apt-get update && sudo apt-get install -y tree

      - name: Generate tree (clean structure)
        run: |
          tree --dirsfirst \
               -I '.obsidian|.github|attachments|node_modules|.git|*.log|*.tmp' \
               -L 6 > TREE.md

      - name: Replace tree in README.md
        run: python3 .github/scripts/replace_tree.py

      - name: Commit & Push updated README
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit_message: "chore: auto update folder tree 🌳"
          file_pattern: README.md